
//document.ready
$( document ).ready(function() {
    console.log( "ready!" );

// listen for input from user for value
$('click').click(funcion(){
  .html("")
});
//check is number

//place for number..div num


//value is user input
var value = $(num selected by user);

function isPrime(value) {
    for(var i = 2; i < value; i++) {
        if(value % i === 0) {
            return false;
        }
    }
    return value > 1;
}

isPrime();



});
